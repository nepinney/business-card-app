import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    header: {
        fontSize: 40,
        textAlign: 'center',
    },
    helper: {
        fontSize: 20,
        textAlign: 'center',
    }
});